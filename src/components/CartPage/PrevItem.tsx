import React from 'react';
import { ColorData } from '../../api/Queries';

interface Props {
  color: ColorData;
}
const PrevItem = (props: Props) => {
  return (
    <div>

    </div>
  );
};

export default PrevItem;