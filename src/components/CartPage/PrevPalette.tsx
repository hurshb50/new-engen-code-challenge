import React from 'react';
import { ColorData } from '../../api/Queries';

interface Props {
  colors: ColorData[];
  title: string;
}

const PrevPalette = () => {
  return (
    <div>

    </div>
  );
};

export default PrevPalette;