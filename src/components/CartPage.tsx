import React from 'react';
import { CartData } from '../App';

interface Props {
  cart: CartData;
}

const CartPage = ({ cart }: Props) => {
  return (
    <div>

    </div>
  );
};

export default CartPage;